var nodemiral = require('./');

var taskList = nodemiral.taskList('Setup Stud');
// taskList.execute('execute command', {command: 'echo hello'});
// taskList.copy('Copy', {src: './README.md', dest: '~/aa.md'});
taskList.executeScript("EXEC", {script: './aa.sh'});

var session = nodemiral.session('192.241.170.33', {username: 'root', password: 'wddoqpcmuumx'});
session.setTimeout(5000);

taskList.run(session, function() {
  console.log(JSON.stringify(arguments, null, 4));
});